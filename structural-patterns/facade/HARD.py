# Build an 'OrderSystemFacade'. It must interface with three separate sub-systems: 
# 'Inventory' (to check beans), 'StripePayment' (to charge money), and 'Kitchen' 
# (to print the ticket). Handle any sub-system failures within the Facade.