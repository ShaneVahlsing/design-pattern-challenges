# Create a 'PaymentAdapter' that allows your shop to accept both 'ApplePay' 
# and 'LegacyCash' objects, even though they have entirely different 
# data structures (e.g., Token-based vs. Amount-based).